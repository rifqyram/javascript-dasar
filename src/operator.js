/**
 * Operator
 * (+, -, *, /, %, ++, --, +=, -=, *=, /=, %=)
 * 
 */

let tambah = 10 + 10;
let kurang = 10 - 10;
let kali = 10 * 10;
let bagi = 10 / 10;
let sisa = 10 % 4;

// console.log(tambah, kurang, kali, bagi, sisa);

let tambah2 = 10;
let kurang2 = 10;

let x = 0;

// console.log(x++);
// console.log(x);

/** Naming Convension
 * - camelCase -> untuk membuat variabel, function
 * - PascalCase -> untuk membuat class
 * - snake_case -> untuk membuat variable, function
 * - kebab-case -> untuk css, html
 */

/**
 * >, <, >=, <=, ==, ===, !=, !==
 * == -> mengecek apakah nilai sama atau tidak
 * === -> mengecek apakah nilai sama dan tipe data sama atau tidak
 * != -> mengecek apakah nilai tidak sama
 * !== -> mengecek apakah nilai tidak sama dan tipe data tidak sama
 */

let number = 100;
let number2 = '100';
let helo = 'Hello';
let sayHelo = 'Hello';

let result = number !== number2;
// console.log(result);

// Operator AND
console.log(true && true); // true
console.log(true && false); // false
console.log(false && true); // false
console.log(false && false); // false

// Operator OR
console.log(true || true); // true
console.log(true || false); // true
console.log(false || true); // ture
console.log(false || false); // false
